import {connect} from 'react-redux';
import TabRouter from "../components/tab-router";

export default connect(
    (state, props) => ({

    }),
    (dispatch, props) => ({

    })
)(TabRouter);
